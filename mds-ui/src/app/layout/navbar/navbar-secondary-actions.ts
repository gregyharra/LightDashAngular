/**
 * Navbar secondary-action stubs (Help / Notifications).
 * Disabled placeholders until real features are wired — keep code, toggle visibility.
 * Settings is always shown in the shell as a circular navy button → `/settings`.
 */
export const NAVBAR_SHOW_HELP = false;
export const NAVBAR_SHOW_NOTIFICATIONS = false;
