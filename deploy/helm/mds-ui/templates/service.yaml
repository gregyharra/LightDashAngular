apiVersion: v1
kind: Service
metadata:
  name: {{ include "mds-ui.fullname" . }}
  labels:
    {{- include "mds-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mds-ui.selectorLabels" . | nindent 4 }}
